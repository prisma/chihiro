query NestedPagination {
  findManyUser(first: 100) {
    id
    firstName
    lastName
    Post(skip: 5, orderBy: id_DESC) {
      id
      content
      Comment(first: 3, orderBy: id_DESC) {
        id
        content
      }
    }
  }
}
