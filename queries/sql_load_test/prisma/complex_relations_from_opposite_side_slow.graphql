query ComplexRelationsFromOppositeSide {
  findManyComment(where: { id_lt: 100 }) {
    id
    content
    post {
      id
      content
      author {
        id
        firstName
        lastName
      }
    }
  }
}
