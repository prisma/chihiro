query {
  findManyArtist(where: { id: $artist_id }) {
    Albums {
      Title
    }
  }
}


# does a unique index that is not the primary key have direct access to the primary id or does it need to go to disk?
